/**
 * Created by joshoy on 15/8/10.
 */

angular.module(COFIG_APP_NAME).service('projectService',
  function categoryService($localStorage, $location, $rootScope, $q, modalService, sessionService) {

    function listCurrentUserProjects() {

    }

  }
);
